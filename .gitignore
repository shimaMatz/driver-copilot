# Local environment (secrets). Use *.example files as templates only.
.env
.env.*
!.env.example

expo/.env
!expo/.env.example

node_modules

# README スクリプトが生成する一時 Web ビルド（PNG のみコミット）
docs/readme/.web-build/